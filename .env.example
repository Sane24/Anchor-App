# Copy this file to .env, then restart the dev server.
# .env is gitignored — every teammate needs their own copy.
#
# This is the OAuth *client* ID. It is not a secret: it ships inside the
# browser bundle. The client secret is never used by this app.
VITE_GOOGLE_CLIENT_ID=870893688043-le86bdqdft3g4madkphhsilo0nh2a8t7.apps.googleusercontent.com
